/**
 * Immutable user-preference model and persistence.
 *
 * <p>The package defines a storage port and a JSON implementation that tolerates
 * missing files, incomplete older versions, and corrupt data.</p>
 */
package com.discordaudioguard.config;

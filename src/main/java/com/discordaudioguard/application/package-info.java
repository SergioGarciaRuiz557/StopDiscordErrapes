/**
 * Application startup and use-case coordination.
 *
 * <p>This layer composes configuration, audio, and UI components, publishes immutable
 * state, and prevents the UI from depending on real-time engine details.</p>
 */
package com.discordaudioguard.application;
